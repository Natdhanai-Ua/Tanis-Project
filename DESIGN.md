# แนวคิดการออกแบบ — TANIS ATLAS

เอกสารนี้บันทึกการตัดสินใจด้านการออกแบบทั้งหมด เพื่อให้แก้ไขต่อยอดได้อย่างสม่ำเสมอ

---

## 1. แนวคิดหลัก — “หอสังเกตการณ์ภูมิศาสตร์ดิจิทัล”

เว็บไซต์ไม่ได้วางตัวเป็นบล็อกท่องเที่ยวหรือรายงานหน้าเดียว แต่วางตัวเป็น **เครื่องมือสำหรับอ่านโลก**
ผู้ชมนั่งอยู่หน้า “คอนโซล” ที่มีลูกโลกเรืองแสงเป็นศูนย์กลาง ข้อมูลถูกนำเสนอด้วยความแม่นยำแบบเครื่องมือวิทยาศาสตร์
— พิกัดเป็นตัวเลขฟอนต์โมโนสเปซ แผงข้อมูลมีป้ายกำกับชัดเจน สีของสัญญาณดาวเทียมแยกจากสีของภาพถ่ายพื้นผิว

หลักคิดสามข้อที่คุมทั้งงาน

1. **แผนที่คือตัวงาน** — ไม่ใช่ภาพประกอบ แผนที่กินพื้นที่เต็มส่วน วาดด้วย SVG ที่ออกแบบเองทุกเส้น
   (ไม่ใช่แผนที่ไทล์สีเทาแบบที่เห็นทั่วไป) ส่วนอื่นของเว็บไซต์มีไว้รองรับแผนที่
2. **RS และ GNSS เป็นอัตลักษณ์ทางสายตา ไม่ใช่แค่หัวข้อ** — RS ใช้สีเขียวน้ำทะเล (เซนเซอร์)
   GNSS ใช้สีม่วง (สัญญาณ) ใช้สี ไอคอน และรูปทรงแผงเดียวกันทุกที่ ครูจึงเห็นทันทีว่าสองหัวข้อนี้ถูกทำครบ
3. **ความยับยั้งชั่งใจคือความหรู** — สีเน้นหลักมีสีเดียว ระยะห่างเอื้อเฟื้อ อนิเมชันน้อยแต่มีเหตุผล

---

## 2. ชุดสี

กำหนดไว้ที่ `src/styles/tokens.css` — แก้ที่เดียวเปลี่ยนทั้งเว็บไซต์

| บทบาท | โหมดมืด (ค่าเริ่มต้น) | โหมดสว่าง |
|---|---|---|
| พื้นหลัง | `#070C18` | `#F6F7FA` |
| พื้นผิวการ์ด | `#0E1628` | `#FFFFFF` |
| เส้นขอบ | `rgba(255,255,255,.09)` | `rgba(11,18,32,.10)` |
| ตัวอักษรหลัก | `#EEF2F9` | `#0B1220` |
| ตัวอักษรรอง | `#8F9DB6` | `#5A6579` |
| **สีเน้น (Atlas Gold)** | `#E9B44C` | `#A9741D` |
| **RS** | `#3ED6C0` | `#0E8F80` |
| **GNSS** | `#9B87F5` | `#5F4BC4` |
| แผ่นดินบนแผนที่ | `#17233E` | `#D3DBE8` |

**ทำไมเป็นสีทอง** — ให้ความรู้สึกของเครื่องมือวัดทองเหลืองและแผนที่โบราณ แยกออกจากสีของ RS และ GNSS ได้ชัด
และยังมองเห็นได้ดีบนโปรเจกเตอร์ที่สีซีด

**ทำไมมีโหมดสว่าง** — โปรเจกเตอร์ในห้องเรียนบางเครื่องแสดงสีเข้มได้แย่มาก โหมดสว่างจึงเป็นแผนสำรองในวันนำเสนอ
ค่าที่เลือกจะถูกจดจำไว้ในเบราว์เซอร์

---

## 3. ตัวอักษร

| การใช้งาน | ฟอนต์ | เหตุผล |
|---|---|---|
| หัวข้อ / ปุ่ม / เมนู | **IBM Plex Sans Thai** 600–700 | รูปทรงเรขาคณิต ดูเป็นทางการ อ่านง่ายบนจอใหญ่ |
| เนื้อความ | **Sarabun** 400–500 · line-height 1.8 | อักษรไทยต้องการช่องว่างสำหรับสระบนและล่าง |
| พิกัด / ตัวเลข / ลำดับ | **IBM Plex Mono** | ให้ความรู้สึกเป็นข้อมูลเชิงวิทยาศาสตร์ ตัวเลขเรียงตรงกัน |

**ข้อควรระวังของภาษาไทย** — ไม่ใช้ `letter-spacing` กว้างกับข้อความไทย เพราะทำให้สระและวรรณยุกต์ดูหลุดจากพยัญชนะ
ระยะห่างตัวอักษรกว้างสงวนไว้สำหรับตัวเลขและอักษรละตินเท่านั้น

**ระบบตัวเลข** — เลขไทย (๗) ใช้ในเนื้อความ ส่วนเลขอารบิก (07) ใช้ในบริบทข้อมูล เช่น พิกัด ลำดับ และป้ายกำกับ

---

## 4. โครงสร้างเว็บไซต์

หน้าเดียว เลื่อนต่อเนื่อง พร้อมแผงรายละเอียดที่เปิดทับ — เร็วและเหมาะกับการนำเสนอสด ไม่ต้องรอโหลดหน้าใหม่

```
หน้าแรก → แผนที่โลก → สถานที่ทั้งหมด → RS & GNSS คืออะไร → ผู้รับผิดชอบ → ส่วนท้าย
              └─ แผงรายละเอียดสถานที่ (7 ชิ้น)
```

---

## 5. เมนูนำทาง

แถบเมนูลอยด้านบนแบบกระจกฝ้า มีตราสัญลักษณ์เข็มทิศ + `TANIS ATLAS`
ลิงก์ `หน้าแรก · แผนที่โลก · สถานที่ · RS & GNSS · ผู้รับผิดชอบ` และปุ่มสลับโหมดสี
ตำแหน่งที่กำลังอ่านถูกตรวจด้วย `IntersectionObserver` แล้วขีดเส้นใต้สีทอง
บนมือถือยุบเป็นปุ่มแฮมเบอร์เกอร์เปิดเมนูเต็มจอ

---

## 6. หน้าแรก

แบ่งซ้าย–ขวา ซ้ายเป็นข้อความ ขวาเป็นลูกโลก

- ป้ายนำ: รายวิชา · ปีการศึกษา
- หัวเรื่องใหญ่: **“สำรวจโลกผ่านสายตาดาวเทียม”** โดยครึ่งหลังไล่สีทอง
- ย่อหน้าแนะนำโครงงาน 3 บรรทัด
- ปุ่ม `เริ่มสำรวจแผนที่` (ทึบสีทอง) และ `ดูสถานที่ทั้งหมด` (โปร่ง)
- แถบสถิติ: **๗ สถานที่ · ๗ ประเทศ · ๖ ทวีป · RS · GNSS** (คำนวณจากข้อมูลจริงอัตโนมัติ)
- ลูกโลกโครงลวดแบบ orthographic หมุนช้า ๆ มีจุดสีทองของทั้ง 7 สถานที่ที่โผล่/หายตามการหมุน

---

## 7. ส่วนแผนที่โลก

- **แถบซ้าย (320px)** — รายการ 7 สถานที่ พร้อมลำดับ ภาพย่อ ชื่อ ประเทศ และพิกัด
  ชี้แล้วหมุดบนแผนที่สว่างขึ้น คลิกแล้วแผนที่บินไปหา
- **ผืนแผนที่** — โปรเจกชัน Natural Earth วาดเป็น SVG มีเส้นละติจูด–ลองจิจูดบาง ๆ และแสงเรืองใต้ผืนน้ำ
- **หมุด** — วงกลมสีทองพร้อมวงพัลส์ ชี้แล้วขึ้นป้ายชื่อ + การ์ดตัวอย่าง คลิกแล้วซูมเข้าและเปิดรายละเอียด
- **แผงควบคุม** — `+ / − / รีเซ็ตมุมมอง` พร้อมเปอร์เซ็นต์การซูม และคำแนะนำการใช้งาน
- **บนมือถือ** — แถบรายการเปลี่ยนเป็นการ์ดเลื่อนแนวนอนใต้แผนที่ และหมุดถูกขยาย 1.85 เท่าให้แตะง่าย

---

## 8. แผงรายละเอียดสถานที่

เลื่อนเข้าจากขวาบนเดสก์ท็อป (กว้าง 720px) และเลื่อนขึ้นจากล่างบนมือถือ — ไม่เปลี่ยนหน้า จึงกลับสู่แผนที่ได้ทันที

1. ภาพสถานที่เต็มความกว้าง มีเงาไล่ระดับ
2. ลำดับ · ประเทศ · ทวีป · (ป้าย “ข้อมูลตัวอย่าง” ถ้ายังไม่ใส่ข้อมูลจริง)
3. ชื่อไทย + ชื่อทางการ
4. แถบพิกัด: ละติจูด · ลองจิจูด · ทวีป
5. แท็บ: `ภาพรวม · การสำรวจระยะไกล (RS) · ระบบนำทางด้วยดาวเทียม (GNSS) · ข้อมูลภูมิศาสตร์`
6. ส่วนท้ายค้างอยู่เสมอ: ชื่อผู้รับผิดชอบ + ปุ่ม `ก่อนหน้า / ถัดไป` (เดินครบ 7 แห่งได้โดยไม่ต้องปิด)

ปิดด้วย `Esc`, คลิกพื้นหลัง หรือปุ่ม `กลับสู่แผนที่`

---

## 9. การนำเสนอ RS / GNSS

แต่ละสถานที่มีแผงของตัวเอง แยกด้วยสีและไอคอน

- **RS** — ขอบและไอคอนสีเขียวน้ำทะเล หัวข้อย่อย: `ข้อมูลที่ตรวจวัด · ดาวเทียม/เซนเซอร์ · การนำไปใช้`
- **GNSS** — ขอบและไอคอนสีม่วง หัวข้อย่อย: `การระบุตำแหน่ง · ระบบดาวเทียม · การนำไปใช้`

นอกจากนี้ยังมีส่วน **“RS และ GNSS คืออะไร”** อธิบายหลักการทั่วไปของทั้งสองเทคโนโลยี
ส่วนนี้คือสิ่งที่ยกระดับงานจาก “รายการสถานที่” ขึ้นเป็น “เว็บไซต์ภูมิสารสนเทศ”

---

## 10. ส่วนผู้รับผิดชอบ

สองรูปแบบจากข้อมูลชุดเดียวกัน แก้ที่เดียวเปลี่ยนทั้งสองที่

- **การ์ด** — อักษรย่อในวงกลมสีทอง ชื่อ บทบาท ชั้น/เลขที่ และชิปสถานที่ที่รับผิดชอบ (คลิกเปิดรายละเอียดได้)
- **ตาราง** — `ลำดับ · ผู้รับผิดชอบ · สถานที่ · ประเทศ · ทวีป` สำหรับครูที่อยากเห็นภาพรวมทีเดียว

---

## 11. การแสดงผลบนอุปกรณ์ต่าง ๆ

จุดเปลี่ยนที่ 640 / 980 / 1200 / 1600 พิกเซล

| อุปกรณ์ | การปรับ |
|---|---|
| โทรศัพท์ (< 640) | แผงรายละเอียดเป็น bottom sheet · การ์ดเรียงคอลัมน์เดียว · หมุดใหญ่ขึ้น · แผนที่สูงขึ้น |
| แท็บเล็ต (< 980) | เมนูเป็นแฮมเบอร์เกอร์ · แถบรายการเป็นการ์ดเลื่อนแนวนอน · หน้าแรกเรียงลงล่าง |
| แล็ปท็อป (≥ 980) | เค้าโครงเต็มรูปแบบ |
| จอนำเสนอ (≥ 1600) | ขยายความกว้างเนื้อหาและแถบรายการ ให้แผนที่ใหญ่ขึ้น |

ปุ่มและพื้นที่กดทั้งหมดมีขนาดอย่างน้อย 44px และหน้าเว็บไม่เลื่อนแนวนอนในทุกความกว้าง

---

## 12. อนิเมชันและการโต้ตอบ

ใช้เท่าที่มีความหมาย ไม่มีอะไรเด้งหรือหมุนเพื่อความสวยอย่างเดียว

| องค์ประกอบ | รายละเอียด |
|---|---|
| หมุดบนแผนที่ | วงพัลส์ 2.8 วินาที เหลื่อมเวลากันเล็กน้อย |
| การบินไปยังสถานที่ | 700ms ด้วย ease-in-out cubic |
| แผงรายละเอียด | เลื่อนเข้า 420ms |
| เนื้อหาตอนเลื่อนหน้า | ยกขึ้น 14px + จางเข้า ครั้งเดียว |
| การ์ดตอนชี้ | ยกขึ้น 4px + ภาพขยาย 1.05 |
| ลูกโลกหน้าแรก | หมุนต่อเนื่องช้า ๆ |

ทั้งหมดถูกปิดอัตโนมัติเมื่อผู้ใช้ตั้งค่า `prefers-reduced-motion: reduce`

---

## 13. เทคโนโลยีที่เลือกใช้ และเหตุผล

| ส่วน | เลือกใช้ | เหตุผล |
|---|---|---|
| โครงสร้าง | Vite + React 18 + TypeScript | จัดระเบียบโค้ดเป็นคอมโพเนนต์ · TypeScript ช่วยกันข้อมูลผิดรูปแบบ · สร้างไฟล์สแตติกได้ |
| แผนที่ | `d3-geo` + `topojson-client` + ข้อมูล Natural Earth ที่เก็บไว้ในโครงการ | **ไม่ต้องต่ออินเทอร์เน็ต ไม่ต้องใช้ API key** และออกแบบหน้าตาแผนที่ได้เองทั้งหมด |
| สไตล์ | CSS เขียนเองพร้อม design token | หลีกเลี่ยงหน้าตาแบบเทมเพลตสำเร็จรูป และอ่านโค้ดง่ายกว่าคลาสยาว ๆ |
| อนิเมชัน | CSS transition ล้วน | ไม่เพิ่มไลบรารีโดยไม่จำเป็น |
| ฟอนต์ | เก็บไฟล์ไว้ในโครงการ | เว็บไซต์แสดงผลถูกต้องแม้ Wi-Fi โรงเรียนใช้ไม่ได้ |

รวมแพ็กเกจตอนรันจริงเพียง 4 ตัว

---

## 14. ทำไมการออกแบบนี้จึงเหมาะกับโครงงานนี้

การออกแบบนี้แก้ความเสี่ยงจริงสองข้อของเว็บไซต์โครงงานนักเรียน

**หนึ่ง — หน้าตาซ้ำกับคนอื่น** แผนที่ SVG ที่วาดเองทำให้เว็บไซต์ไม่เหมือนงานที่ใช้เทมเพลตหรือฝัง Google Maps
ธีมหอสังเกตการณ์สีเข้มดูโดดเด่นบนจอโปรเจกเตอร์ และตัวอักษรไทยถูกจัดอย่างพิถีพิถัน

**สอง — พังในวันนำเสนอ** ทั้งแผนที่และฟอนต์ถูกเก็บไว้ในโครงการทั้งหมด เว็บไซต์จึงทำงานได้แม้ไม่มีอินเทอร์เน็ต
และยังมีโหมดสว่างเผื่อโปรเจกเตอร์แสดงสีเข้มได้ไม่ดี

นอกจากนี้ RS และ GNSS ยังได้อัตลักษณ์ทางสายตาถาวร ทำให้เห็นชัดว่าข้อกำหนดของโครงงานถูกทำครบ
และเนื้อหาทั้งหมดอยู่ในไฟล์ข้อมูล 2 ไฟล์ที่มีคำอธิบายกำกับ การแทนที่ข้อมูลจริงจึงใช้เวลาไม่นาน
และไม่ต้องแก้โค้ดส่วนแสดงผลเลย
